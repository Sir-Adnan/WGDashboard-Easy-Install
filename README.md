# 🚀 اسکریپت نصب آسان WGDashboard (WGDashboard Easy Install)

این پروژه یک اسکریپت **Bash** ساده و خودکار برای نصب و راه‌اندازی سریع [WGDashboard](https://github.com/WGDashboard/WGDashboard) بر روی سرورهای لینوکسی (به ویژه اوبونتو) است. 

با استفاده از این اسکریپت، دیگر نیازی به درگیری با دستورات پیچیده داکر یا تنظیمات دستی فایل‌های کانفیگ ندارید. همه چیز به صورت خودکار انجام می‌شود.

---

## ✨ ویژگی‌ها

* 📦 **نصب خودکار پیش‌نیازها:** بررسی و نصب خودکار Docker و Docker Compose در صورت عدم وجود.
* ⚙️ **پیکربندی تعاملی:** دریافت پورت‌ها، نام کاربری و رمز عبور دلخواه از شما در حین نصب.
* 🛡️ **تنظیمات امنیتی:** تنظیم خودکار دسترسی‌های لازم (`NET_ADMIN`) برای وایرگارد.
* 🔄 **همیشه به‌روز:** دریافت آخرین نسخه ایمیج WGDashboard از گیت‌هاب.
* wall **فایروال:** (اختیاری) باز کردن پورت‌های مورد نیاز در UFW.

---

## 📥 نحوه نصب و اجرا

برای نصب، تنها کافیست دستور زیر را در ترمینال سرور خود اجرا کنید. این دستور اسکریپت را دانلود و اجرا می‌کند:

### روش اول (پیشنهادی - با curl)
```bash
bash <(curl -sL https://raw.githubusercontent.com/Sir-Adnan/WGDashboard-Easy-Install/main/wg.sh)

```

### روش دوم (اگر curl نصب نبود)

```bash
wget -qO- (curl -sL https://raw.githubusercontent.com/Sir-Adnan/WGDashboard-Easy-Install/main/wg.sh) | bash

```

---
## 📋 مراحل نصب

پس از اجرای دستور بالا، اسکریپت موارد زیر را از شما می‌پرسد (می‌توانید با زدن Enter مقادیر پیش‌فرض را قبول کنید):

1. **Public IP:** آدرس آی‌پی سرور (به صورت خودکار تشخیص داده می‌شود).
2. **Dashboard Username:** نام کاربری پنل مدیریت (پیش‌فرض: `admin`).
3. **Dashboard Password:** رمز عبور پنل (ورودی مخفی است).
4. **Dashboard Port:** پورتی که پنل وب روی آن اجرا می‌شود (پیش‌فرض: `10086`).
5. **WireGuard Port:** پورت UDP برای اتصال کلاینت‌های وایرگارد (پیش‌فرض: `51820`).

---

## 🚀 دسترسی به پنل

بعد از اتمام نصب، می‌توانید از طریق مرورگر به آدرس زیر بروید:

```
http://YOUR_SERVER_IP:10086

```

* **نام کاربری:** (همانی که در نصب وارد کردید - پیش‌فرض: `admin`)
* **رمز عبور:** (همانی که در نصب وارد کردید)

---

## 🛠️ دستورات مفید داکر

برای مدیریت کانتینر نصب شده می‌توانید از دستورات زیر استفاده کنید:

**مشاهده وضعیت کانتینر:**

```bash
docker ps

```

**مشاهده لاگ‌ها (برای عیب‌یابی):**

```bash
docker logs wgdashboard

```

**توقف سرویس:**

```bash
docker stop wgdashboard

```

**شروع مجدد سرویس:**

```bash
docker start wgdashboard

```
## 🔄 بروزرسانی و ارتقا (Update)

از آنجایی که این نصب بر پایه **Docker** انجام شده است، دریافت آخرین تغییرات و آپدیت‌های WGDashboard بسیار ساده است و **هیچ‌گونه اطلاعاتی (مانند یوزرها، کلیدها و تنظیمات) پاک نخواهد شد**.

برای آپدیت به آخرین نسخه، کافیست مراحل زیر را در ترمینال سرور انجام دهید:

1.  **وارد پوشه نصب شوید:**
    ```bash
    cd /opt/wgdashboard
    ```

2.  **آخرین نسخه ایمیج را دانلود کنید:**
    ```bash
    docker compose pull
    ```

3.  **کانتینر را با نسخه جدید بازسازی کنید:**
    ```bash
    docker compose up -d
    ```

4.  **بررسی کنید که همه چیز درست کار می‌کند:**
    ```bash
    docker ps
    ```
    *(اگر در ستون STATUS عبارت Up را دیدید، یعنی آپدیت با موفقیت انجام شده است.)*

---

---

## ❤️ اعتبار و منابع

این اسکریپت صرفاً یک نصب‌کننده خودکار است. پروژه اصلی WGDashboard توسط توسعه‌دهندگان آن ساخته شده است.

* لینک پروژه اصلی: [WGDashboard GitHub](https://github.com/WGDashboard/WGDashboard)
* ایمیج داکر: `ghcr.io/wgdashboard/wgdashboard`
